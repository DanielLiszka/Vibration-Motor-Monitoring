# DriftDetector

## Files

- `include/DriftDetector.h`
- `src/DriftDetector.cpp`

## Overview

Detects distribution drift between baseline behavior and current data.

## Configuration

See `include/Config.h` for related feature toggles, limits, and defaults.
