# ModelManager

## Files

- `include/ModelManager.h`
- `src/ModelManager.cpp`

## Overview

Loads, stores, and selects models used by the inference subsystem.

## Configuration

See `include/Config.h` for related feature toggles, limits, and defaults.
