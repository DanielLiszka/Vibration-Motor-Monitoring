# Cloud

Optional cloud-side components used for data collection, monitoring, and model lifecycle workflows.

This code is not required to build or run the ESP32 firmware, and is provided as a reference implementation for integrations.

## Install Dependencies

```bash
python -m pip install -r cloud/requirements.txt
```
