# Logging

The firmware can emit diagnostics over serial and (optionally) publish telemetry over the network.

Logging behavior is controlled via `include/Config.h`.

See `include/DataLogger.h` and `src/DataLogger.cpp`.
