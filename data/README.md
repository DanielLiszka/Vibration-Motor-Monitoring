# Web Assets

This folder contains the static web assets served by the ESP32 web dashboard when the web server feature is enabled.

- `index.html` - Dashboard UI
- `style.css` - Dashboard styling
- `app.js` - Dashboard logic (WebSocket updates and rendering)
