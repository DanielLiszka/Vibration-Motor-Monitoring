# Web Dashboard

When the web server is enabled, the ESP32 hosts a small dashboard for real-time status and visualization.

## Features

- Live feature values and fault state
- Spectrum visualization via WebSockets

See `include/WebServer.h` and `src/WebServer.cpp`.
