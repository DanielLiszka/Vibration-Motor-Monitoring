# Tools

This folder contains helper tooling used during development and data work.

- `generate_test_data.py` - Generate synthetic vibration data for local testing.
- `train_model.py` - Train/export on-device models used by the firmware.
- `ml/` - Reusable ML utilities (preprocessing, training, export).
