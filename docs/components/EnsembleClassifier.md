# EnsembleClassifier

## Files

- `include/EnsembleClassifier.h`
- `src/EnsembleClassifier.cpp`

## Overview

Combines multiple classifier outputs into a single decision.

## Configuration

See `include/Config.h` for related feature toggles, limits, and defaults.
