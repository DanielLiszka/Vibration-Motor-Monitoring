# Scripts

Helper utilities for monitoring and analyzing vibration data.

- `mqtt_monitor.py` - Subscribe to MQTT topics and display live telemetry.
- `data_analyzer.py` - Offline analysis and plotting for captured data.

## Install Dependencies

```bash
python -m pip install -r scripts/requirements.txt
```
