# OTA Updates

Over-the-air updates let you flash new firmware over WiFi.

OTA is optional and can be enabled in `include/Config.h`. If you enable it, set `OTA_PASSWORD` to a non-empty value before flashing.

See `include/OTAUpdater.h` and `src/OTAUpdater.cpp`.
