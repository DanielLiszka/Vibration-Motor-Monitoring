# Config

## Files

- `include/Config.h`
- (implementation varies)

## Overview

Defines build-time configuration defaults and feature toggles for the firmware.

## Configuration

See `include/Config.h` for related feature toggles, limits, and defaults.
